function potencia(a, b) {
  return Math.pow(a, b);
}

function maior(a, b) {
  return Math.max(a, b);
}

function menor(a, b) {
  return Math.min(a, b);
}

function raiz(numero) {
  return Math.sqrt(numero);
}

module.exports = { potencia, maior, menor, raiz };
